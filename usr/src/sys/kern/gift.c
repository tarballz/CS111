#include <sys/sysproto.h>


/* Define body of syscall
int sys_gift(struct thread *td, struct gift_args *args) {
	//printf("Gifting %d tickets from %d to %d\n", args->t, args->pid, td->td_ucred->cr_uid);
	// Todo: Actual implementation
	sfsjoifhsoeifjesoi
	return 0;
}
 */